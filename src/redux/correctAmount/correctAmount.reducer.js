const INITIAL_STATE = {
  correctAmount: 0,
};

const correctAmountReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default correctAmountReducer;
