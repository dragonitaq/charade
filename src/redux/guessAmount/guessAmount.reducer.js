const INITIAL_STATE = {
  guessAmount: 0,
};

const guessAmountReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default guessAmountReducer;
